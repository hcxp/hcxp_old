<p class="text-muted">
Invitations are unlimited, but persons you invite will be associated with your
account in the <a href="/u">user tree</a> and you may be responsible for them
if they cause problems.  Please use your discretion when inviting persons you
don't personally know.
</p>

<%= form_tag "/invitations", :method => :post, class: 'form-horizontal' do |f| %>
  <% if defined?(return_home) && return_home %>
    <%= hidden_field_tag :return_home, 1 %>
  <% end %>

  <div class="form-group">
    <%= label_tag :email, "E-mail Address:", :class => "required col-sm-2 control-label" %>
    <div class="col-sm-3">
      <%= text_field_tag :email, "", :size => 30, :autocomplete => "off", class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <%= label_tag :memo, "Memo to User:", :class => "required col-sm-2 control-label" %>
    <div class="col-sm-3">
      <%= text_field_tag :memo, "", :size => 60, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
      <%= submit_tag "Send Invitation", class: 'btn btn-default' %>
    </div>
  </div>
<% end %>

